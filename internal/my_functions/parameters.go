package my_functions

// Counter to track the new players and set their ID (negative)
var NewPlayerCount int = -1

// Counter to track the new teams and set their ID (negative)
var NewTeamCount int = -1

// Counter to track the new clubs and set their ID (negative)
var NewClubCount int = -1
